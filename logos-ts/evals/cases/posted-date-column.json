{
  "name": "posted-date-column",
  "codebase": "../../demos/hn-jobs",
  "comment": {
    "target": "component:DirectoryPage",
    "text": "Add a 'Posted' column as the last column (after Details) showing the job's postedAt date formatted as YYYY-MM-DD. Do not use the existing human-readable formatDate helper for this column; render an ISO date such as j.postedAt.toISOString().slice(0, 10).",
    "component": "DirectoryPage",
    "storyId": "run:root-app:/",
    "runTargetId": "root-app",
    "appPath": "/",
    "label": "table · job columns"
  },
  "agent": "implementation",
  "tier": "deterministic",
  "repeat": 3,
  "checks": {
    "typecheck": {
      "cwd": "",
      "cmd": [
        "pnpm", "exec", "tsc",
        "--noEmit"
      ]
    },
    "behavior": {
      "oracle": [
        "../checks/posted-date-column.check.test.tsx",
        "../checks/source-text.ts"
      ],
      "cwd": "",
      "cmd": [
        "node", "--import", "tsx",
        "--test",
        "posted-date-column.check.test.tsx"
      ]
    }
  }
}
