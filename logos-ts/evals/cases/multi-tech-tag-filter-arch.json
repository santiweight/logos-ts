{
  "name": "multi-tech-tag-filter-arch",
  "codebase": "../../demos/hn-jobs",
  "comment": {
    "target": "component:DirectoryPage",
    "label": "DirectoryPage",
    "component": "DirectoryPage",
    "text": "Allow users to select more than one Tech tag at a time. Support repeated tag URL params and comma-separated tag values, match jobs that include every selected tag, keep single-tag URLs working, and keep the DirectoryPage filter links/forms wired to preserve existing search and filters."
  },
  "agent": "architecture",
  "tier": "capability",
  "checks": {
    "typecheck": { "cwd": "", "cmd": ["pnpm", "exec", "tsc", "--noEmit"] },
    "behavior": {
      "oracle": "../checks/multi-tech-tag-filter.check.test.ts",
      "cwd": "",
      "cmd": ["node", "--import", "tsx", "--test", "multi-tech-tag-filter.check.test.ts"]
    },
    "job-filter-regression": { "cwd": "", "cmd": ["node", "--import", "tsx", "--test", "tests/job-filters.test.ts"] }
  }
}
